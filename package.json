{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "tsdown": "0.22.3"
    }
  },
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "bun test",
    "build": "bun run --parallel --filter='./packages/**' build"
  },
  "devDependencies": {
    "@gameroman/config": "0.2.2",
    "@types/bun": "1.3.14",
    "oxfmt": "0.57.0",
    "oxlint": "1.72.0",
    "oxlint-tsgolint": "0.24.0",
    "typescript": "6.0.3"
  }
}
