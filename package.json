{
  "name": "@websim/monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "tsdown": "^0.17.0"
    }
  },
  "scripts": {
    "lint": "bunx biome lint",
    "format": "bunx biome check --write --linter-enabled=false",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "typescript": "^5.9.3"
  }
}
