{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "tsdown": "^0.21.7"
    }
  },
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "bun test",
    "build": "bun run --parallel --filter='./packages/**' build"
  },
  "devDependencies": {
    "@gameroman/config": "^0.1.0",
    "@types/bun": "^1.3.11",
    "oxfmt": "^0.50.0",
    "oxlint": "^1.57.0",
    "oxlint-tsgolint": "^0.21.0",
    "typescript": "^6.0.2"
  }
}
